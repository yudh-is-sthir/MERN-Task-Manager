{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "description": "A MERN application for basic tasks management",
  "main": "index.js",
  "scripts": {
    "dev-server": "pnpm run dev --prefix backend",
    "dev-client": "pnpm start --prefix frontend",
    "dev": "concurrently \"pnpm run dev-server\" \"pnpm run dev-client\"",
    "install-all": "pnpm install && pnpm install --prefix frontend && pnpm install --prefix backend",
    "build": "pnpm install --prefix frontend && pnpm run build --prefix frontend && pnpm install --prefix backend",
    "start": "cd backend && node app.js"
  },
  "keywords": [
    "MERN",
    "Tasks",
    "Task manager",
    "Full stack application"
  ],
  "author": "Mwenda",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
